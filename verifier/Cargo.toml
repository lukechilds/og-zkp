[package]
name = "og-zkp-verifier"
version = "0.1.0"
edition = "2021"

[dependencies]
og-zkp-core = { path = "../core", features = ["verify"] }
serde_json = "1"
hex = "0.4"

[dev-dependencies]
assert_cmd = "2"
