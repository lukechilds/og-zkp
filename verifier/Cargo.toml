[package]
name = "og-zkp-verifier"
version = "1.0.1"
edition = "2021"

[dependencies]
og-zkp-core = { path = "../core", features = ["verify"] }
anyhow = "1"
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2"
serde_json = "1"
