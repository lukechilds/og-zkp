name: CI

on: push

jobs:
  precache:
    runs-on: ubuntu-latest
    steps:
      - &free-disk
        name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/share/boost

      - &checkout
        name: Checkout
        uses: actions/checkout@v4

      - &cargo-deps-cache
        name: Cache Cargo dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-deps-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-deps-

      - &cargo-target-cache
        name: Cache Cargo targets
        uses: actions/cache@v4
        with:
          path: |
            target
            methods/guest/target
          key: ${{ runner.os }}-cargo-target-${{ hashFiles('**/*', '!web/**', '!**/README.md', '!media/**', '!.git/**') }}
          restore-keys: ${{ runner.os }}-cargo-target-

      - &install-risc0
        name: Install RISC Zero toolchain (rzup)
        run: |
          curl -L https://risczero.com/install | bash
          echo "$HOME/.risc0/bin" >> $GITHUB_PATH
          ~/.risc0/bin/rzup install rust 1.88.0
          ~/.risc0/bin/rzup install cpp 2024.1.5

      - name: Compile root workspace
        run: cargo build --workspace --all-targets --all-features

      - name: Compile guest workspace
        run: cargo build --manifest-path methods/guest/Cargo.toml --all-targets --all-features

  fmt:
    runs-on: ubuntu-latest
    steps:
      - *checkout

      - name: Check root workspace formatting
        run: cargo fmt --all -- --check

      - name: Check guest formatting
        run: cargo fmt --manifest-path methods/guest/Cargo.toml -- --check

  clippy:
    runs-on: ubuntu-latest
    needs: precache
    steps:
      - *free-disk

      - *checkout

      - *cargo-deps-cache

      - *cargo-target-cache

      - *install-risc0

      - name: Run root workspace clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Run guest clippy
        run: cargo clippy --manifest-path methods/guest/Cargo.toml --all-targets --all-features -- -D warnings

  unit:
    runs-on: ubuntu-latest
    needs: precache
    steps:
      - *free-disk

      - *checkout

      - *cargo-deps-cache

      - *cargo-target-cache

      - *install-risc0

      - name: Run unit tests
        run: cargo test --workspace --lib --bin og-zkp

  e2e:
    runs-on: ubuntu-latest
    needs: precache
    steps:
      - *free-disk

      - *checkout

      - *cargo-deps-cache

      - *cargo-target-cache

      - *install-risc0

      - name: Run e2e tests
        run: cargo test --test e2e

      - name: Run real verifier fixture tests
        run: cargo test -p og-zkp-verifier

  e2e-fetch:
    runs-on: ubuntu-latest
    needs: precache
    steps:
      - *free-disk

      - *checkout

      - *cargo-deps-cache

      - *cargo-target-cache

      - *install-risc0

      - name: Run real lookup e2e tests
        run: cargo test --test real_lookup
