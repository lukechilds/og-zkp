[package]
name = "og-zkp"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
og-zkp-core = { path = "../../core" }
risc0-zkvm = { version = "^3.0.5", default-features = false, features = ['std'] }
